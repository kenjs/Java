<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE sqlMap   
    PUBLIC "-//ibatis.apache.org//DTD SQL Map 2.0//EN"   
    "http://ibatis.apache.org/dtd/sql-map-2.dtd">
<sqlMap>
	<!-- 消息列表查询 -->
	<select id="iBatisQueryDriverNotificationInfo" parameterClass="map" resultClass="com.cy.driver.user.domain.DriverNotificationInfoDomain">
		select 
			ID as id,
			DRIVER_ID as driverId,
			CARGO_ID as cagoId,
			TYPE as type,
			DESCRIBE_INFO as describeInfo,
			PUSH_SUCCESS as pushSuccess
		from 
			t_driver_notification_info
		where
			DRIVER_ID = #driverId#
		ORDER BY CREATE_TIME DESC
		LIMIT $fromSize$,$listSize$
	</select>
</sqlMap>